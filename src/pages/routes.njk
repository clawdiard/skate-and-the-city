---
layout: base.njk
title: Routes — Skate And the City
permalink: /routes/
---
<h1 class="text-3xl font-bold text-skate-accent mb-2">🗺️ Curated Routes</h1>
<p class="text-gray-400 mb-8">Pre-planned skate itineraries combining the best spots with transit directions. Pick a route and ride.</p>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  {% for route in allRoutes %}
  <a href="{{ '/routes/' + route.id + '/' | url }}" 
     class="block bg-skate-surface rounded-lg p-5 border border-skate-accent/20 hover:border-skate-accent/60 transition group">
    <h2 class="text-xl font-bold text-white group-hover:text-skate-accent transition mb-2">{{ route.name }}</h2>
    <p class="text-sm text-gray-400 mb-4 line-clamp-2">{{ route.description }}</p>
    <div class="flex flex-wrap gap-3 text-xs text-gray-500">
      <span class="bg-skate-primary px-2 py-1 rounded">⏱️ {{ route.duration }}</span>
      <span class="bg-skate-primary px-2 py-1 rounded">📏 {{ route.distance }}</span>
      <span class="bg-skate-primary px-2 py-1 rounded">{% for i in range(1, 6) %}{% if i <= route.difficulty %}★{% else %}☆{% endif %}{% endfor %}</span>
      <span class="bg-skate-primary px-2 py-1 rounded">📍 {{ route.stops.length }} stops</span>
    </div>
    <p class="text-xs text-gray-500 mt-3">{{ route.borough }}</p>
  </a>
  {% endfor %}
</div>
