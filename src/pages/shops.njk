---
layout: base.njk
title: Skate Shops — Skate And the City
metaDescription: Find the best skateboard shops in NYC — decks, trucks, wheels, and gear.
---

<section class="space-y-8">
  <div class="text-center mb-8">
    <h1 class="text-4xl font-display text-white mb-2">🛹 Skate Shops</h1>
    <p class="text-gray-400 max-w-xl mx-auto">NYC's core shops — the places that keep the scene alive. Support local.</p>
  </div>

  <div class="grid gap-6 sm:grid-cols-2">
    {% for shop in allShops %}
    <a href="{{ ('/shops/' + shop.id + '/') | url }}" class="block bg-skate-surface rounded-lg p-5 border border-skate-accent/20 hover:border-skate-accent/60 transition group">
      <h2 class="text-xl font-semibold text-white group-hover:text-skate-accent transition">{{ shop.name }}</h2>
      <p class="text-sm text-gray-400 mt-1">{{ shop.neighborhood }}, {{ shop.borough }}</p>
      <p class="text-sm text-gray-300 mt-2">{{ shop.description | truncate(120) }}</p>
      <div class="flex flex-wrap gap-2 mt-3">
        {% for spec in shop.specialties %}
        <span class="text-xs bg-skate-accent/10 text-skate-accent px-2 py-0.5 rounded-full">{{ spec }}</span>
        {% endfor %}
      </div>
    </a>
    {% endfor %}
  </div>
</section>
