{
  "name": "skate-and-the-city",
  "version": "1.0.0",
  "description": "Skate And the City — NYC skate spots, routes, shops & food",
  "scripts": {
    "build": "node scripts/build-search-index.js && node scripts/generate-gpx.js && npx @11ty/eleventy && npx tailwindcss -i ./src/assets/css/input.css -o ./_site/assets/css/style.css --minify",
    "dev": "npx @11ty/eleventy --serve",
    "build:css": "npx tailwindcss -i ./src/assets/css/input.css -o ./_site/assets/css/style.css --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^3.4.19"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "fuse.js": "^7.1.0"
  }
}
